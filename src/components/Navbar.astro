<nav>
    <button id="nav-toggle" aria-controls="primary-nav" aria-expanded="false">
        <span class="sr-only">Menu</span>
      </button>
    <a href="/" id="logo-link">
        <span id="logo-text">Finn's Freelancing</span>
    </a>
    <ul id="primary-nav" data-mobile-nav-visible="false">
        <li class="nav-item">
            <a href="#home" class="nav-link">
            <img src="/home2.svg" alt="" class="nav-icon">
            Home
            </a>
        </li>
        <li class="nav-item">
            <a href="" class="nav-link">
                <img src="/info-circle.svg" alt="" class="nav-icon">
                About
            </a>
        </li>
        <li class="nav-item">
            <a href="" class="nav-link">
                <img src="/shooting-star-line.svg" alt="" class="nav-icon">
                Services
            </a>
        </li>
        <li class="nav-item">
            <a href="#projects" class="nav-link">
                <img src="/laptop.svg" alt="" class="nav-icon">
                Projects
            </a>
        </li>
        <!-- <li class="nav-item">
            <a href="" class="nav-link">
                <img src="/coin.svg" alt="" class="nav-icon">
                Pricing
            </a>
        </li> -->
        <li class="nav-item">
            <a href="" class="nav-link">
                <img src="/rocket.svg" alt="" class="nav-icon">
                Contact
            </a>
        </li>
    </ul>
</nav>

<style>
    nav {
        display: flex;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 999;
        /* border: 1px solid black; */
        background-color: black;
        /* background: radial-gradient(circle at 10% 20%, rgb(0, 0, 0) 0%, rgb(64, 64, 64) 90.2%); */
        /* background: linear-gradient(112.1deg, rgb(32, 38, 57) 11.4%, rgb(63, 76, 119) 70.2%); */
        /* background: radial-gradient(circle at 24.1% 68.8%, rgb(50, 50, 50) 0%, rgb(0, 0, 0) 99.4%); */
        color: white;
    }
    a {
        text-decoration: none;
        color: inherit;
    }
    #nav-toggle {
        display: none;
        cursor: pointer;
        z-index: 9999;
    }
    #logo-link {
        padding: 1rem 0 0.5rem 0;
        z-index: 1000;
    }
    #logo-link:hover #logo-text {
        background: linear-gradient(89.7deg, rgb(0, 32, 95) 2.8%, rgb(132, 53, 142) 97.8%);
        background-clip: text;
        -webkit-background-clip: text;
        color: transparent;
    }
    #logo-text {
        font-family: Heyam;
        font-size: 2rem;
        margin-left: 1rem;
        letter-spacing: 1px;
        transition: 350ms;
    }
    #primary-nav {
        display: flex;
        align-items: baseline;
        margin-left: auto;
    }
    .nav-item {
        cursor: pointer;
        font-family: Coolvetica;
        margin: 0 1rem;
        padding: 1rem 0;
        letter-spacing: 1px;
        font-size: 1.1rem;
    }
    .nav-item:hover .nav-icon {
        transform: translate(0px);
    }
    .nav-icon {
        display: inline;
        transform: translateY(5px);
        width: 1.5rem;
        transition: 200ms;
    }
    @media (max-width: 58rem) {
        #nav-toggle {
            display: block;
            position: fixed;
            top: 1rem;
            right: 1rem;
            background-image: url("/menu.svg");
            background-repeat: no-repeat;
            background-position: center;
            background-size: cover;
            width: 2rem;
            height: 2rem;
            background-color: black;
            border: none;
        }
        #nav-toggle[aria-expanded="true"] {
            background-image: url("/x.svg");
        }
        #primary-nav {
            z-index: 999;
            position: fixed;
            background-color: black;
            background: radial-gradient(circle at 24.1% 68.8%, rgb(50, 50, 50) 0%, rgb(0, 0, 0) 99.4%);
            padding-top: 5rem;
            /* Change last percentage for how far menu slides out */
            inset: 0 0 0 40%;
            transform: translateX(100%);
            flex-direction: column;
            align-items: start;
            transition: 500ms ease-in-out;
        }
        #primary-nav[data-mobile-nav-visible="true"] {
            transform: translateX(0%);
        }
        .nav-item {
            padding: 1rem 0;
        }
    }
</style>

<script>
    const primaryNav = document.querySelector("#primary-nav");
    const navToggle = document.querySelector("#nav-toggle");
    navToggle.addEventListener("click", () => {
      console.log("nav toggle clicked");
      const isNavVisible = primaryNav.getAttribute("data-mobile-nav-visible");
      if (isNavVisible === "false") {
        primaryNav.setAttribute("data-mobile-nav-visible", "true");
        navToggle.setAttribute("aria-expanded", "true");
      } else {
        primaryNav.setAttribute("data-mobile-nav-visible", "false");
        navToggle.setAttribute("aria-expanded", "false");
      }
    });
  </script>